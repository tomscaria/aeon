---
id: ISS-029
title: ADR-093 outputs/{skill}/{date}.json contract never shipped — falsifier expired 14 days
status: open
severity: critical
category: plan-drift
detected_by: plan-adherence
detected_at: 2026-05-31T20:30:00Z
resolved_at: null
affected_skills: [monitor-polymarket, polymarket-comments, narrative-tracker]
root_cause: |
  swarm-fund-mvp/DECISIONS.md ADR-093 (2026-05-03) committed that Aeon would ship
  `outputs/{skill}/{date}.json` so swarm-fund's `python/execution/aeon_adapter.py` could
  poll them on 15-min cadence and emit `MarketTicks` with `kind="aeon_signal"`. The ADR
  included an explicit falsifier window: "if Aeon doesn't ship the JSON contract within
  ~2 weeks (~2026-05-17) the wire-up is aspirational."

  Today (2026-05-31) the falsifier has been fired for 14 days. `ls /Users/stew/scaria/aeon/`
  shows only `dashboard/outputs/` (json-render specs for the local dashboard); there is no
  `outputs/{skill}/{date}.json` at the repo root. The aeon_adapter has been 404-ing every
  15 minutes for two weeks. swarm-fund's defect-hardening PRs (#29-32) patched the consumer
  side while the producer never landed.

  Concrete fix: each of `monitor-polymarket`, `polymarket-comments`, `narrative-tracker`
  writes its output to `outputs/{skill}/YYYY-MM-DD.json` alongside the existing notify path.
  Smallest possible diff is one step at the end of each SKILL.md. Aeon adapter unblocks
  immediately.

  NOTE: originally filed as ISS-021 in the plan-adherence manual triage run on 2026-05-31,
  but renumbered to ISS-029 during PR-merge conflict resolution — main had already taken
  ISS-021 for the cost-report 4-consecutive-failures issue filed 2026-05-11. Issue numbers
  are sequential and never reused; the later writer takes the next free slot.
detected_by_run: 2026-05-31 manual plan-adherence triage
fix_pr: https://github.com/tomscaria/aeon/pull/13
---
