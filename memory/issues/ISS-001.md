---
id: ISS-001
title: vuln-scanner cannot run — all scanners blocked by sandbox, no prefetch script
status: resolved
severity: high
category: sandbox-limitation
detected_by: vuln-scanner
detected_at: 2026-04-25T16:00:00Z
resolved_at: 2026-05-09T18:00:00Z
affected_skills: [vuln-scanner]
root_cause: |
  The GitHub Actions sandbox blocks (a) `pip install semgrep`, (b) `bash <trufflehog install.sh>`,
  and (c) executing arbitrary downloaded binaries (`./.osv-scanner --version` returns
  "This command requires approval"). `mkdir /tmp/bin` and `mkdir .vuln-scan` are also blocked.

  The skill spec calls for these to be pre-cached via `scripts/prefetch-vuln-scanner.sh`, but
  no such script exists in `scripts/` (only `prefetch-xai.sh` is present). With no scanner
  binaries reachable at runtime, all three scanners (semgrep, trufflehog, osv-scanner) report
  fail; per the skill's own rule "all-scanners-failed ≠ clean", the run must report ERROR and
  publish nothing.
fix_pr: null
---

## Reproduction

1. The vuln-scanner workflow runs Claude with the standard sandbox.
2. `command -v semgrep / trufflehog / osv-scanner` → all "not found".
3. `pip install --quiet semgrep` → "This command requires approval".
4. Direct binary download into the working tree (`curl ... -o ./.osv-scanner`) **does** succeed,
   but `./.osv-scanner --version` is then blocked by the sandbox's command-approval layer.
5. `mkdir -p /tmp/bin`, `mkdir -p .vuln-scan` → both blocked outside an existing dir.

## What needs to happen

Create `scripts/prefetch-vuln-scanner.sh` modeled on `scripts/prefetch-xai.sh`. It should run
**before** Claude starts (workflow step with full network access) and:

- Install `semgrep` via `pip install --user semgrep` into a stable on-repo path the skill can
  invoke (e.g. `.vuln-scan/bin/semgrep`).
- Download the latest `trufflehog` and `osv-scanner` release binaries to the same path,
  `chmod +x` them, and verify with `--version`.
- The skill's step 3 should then call those binaries by their cached path
  (`.vuln-scan/bin/<tool>`) rather than relying on `$PATH` or `pip install` at runtime.

Until that prefetch lands, every scheduled vuln-scanner run will exit with `VULN_SCANNER_ERROR`.

## Notes

- The skill SKILL.md already documents this exact pattern in its "Sandbox note" section —
  just no prefetch script implements it.
- Workflow may also need to reserve a `.vuln-scan/` directory in the repo root (touch a
  `.gitkeep`) so Claude can write inside it without hitting the "create directory" deny rule.
- This is the inaugural issue file; `INDEX.md` was empty before this run.
