---
title: "Vuln Scanner — 2026-04-25 (ERROR)"
date: 2026-04-25
categories: [article]
source_file: "vuln-scan-2026-04-25.md"
excerpt: "Status: VULN_SCANNER_ERROR — all-scanners-fail. No repository was scanned. No findings published. No PVR opened. No PR opened."
---
# Vuln Scanner — 2026-04-25 (ERROR)

**Status:** `VULN_SCANNER_ERROR` — all-scanners-fail. No repository was scanned. No findings published. No PVR opened. No PR opened.

## Why this run produced no output

The skill's three required scanners — `semgrep`, `trufflehog`, `osv-scanner` — were all unavailable at runtime in this sandbox configuration:

| Tool | Status | Failure mode |
|------|--------|---------------|
| semgrep | fail | `pip install --quiet semgrep` blocked by sandbox approval layer |
| trufflehog | fail | `bash <install.sh> -b /tmp` blocked by sandbox approval layer |
| osv-scanner | fail | binary download to `./.osv-scanner` succeeded, but `./.osv-scanner --version` blocked at execution by the same approval layer |

`scripts/prefetch-vuln-scanner.sh` — the workaround documented in the skill's own *Sandbox note* — does not exist in this repo. Only `scripts/prefetch-xai.sh` is present. With no pre-cached binaries and no runtime install path, every code path of the skill terminates before producing scanner output.

Per the skill's explicit rule ("All-scanners-failed ≠ clean. Report it as an error and do not publish anything."), nothing was disclosed and no repo was forked.

Filed as [memory/issues/ISS-001.md](../memory/issues/ISS-001.md) (severity: high, category: sandbox-limitation).

## Target candidates surveyed (not scanned)

For the record, here are the trending candidates that would have been considered against the skill's selection criteria, drawn from `gh api search/repositories?q=created:>2026-04-11+stars:>50`:

| Candidate | Stars | Lang | Untrusted-input surface | Notes |
|-----------|-------|------|--------------------------|-------|
| `browser-use/browser-harness` | 6,727 | Python | Yes — drives a real browser, executes web content for LLMs | Best-fit candidate; would have been the pick. |
| `h4ckf0r0day/obscura` | 4,139 | Rust | Yes — headless browser for AI agents / scraping | Alt candidate. Username pattern is *suggestive* (worth a SECURITY.md check before forking even when scanners work). |
| `vercel-labs/wterm` | 2,398 | TS | Yes — web terminal emulator | Would need a careful review of escape-sequence handling. |
| `browser-use/video-use` | 4,515 | Python | Partial — file I/O on user-provided videos | Same maintainer as browser-harness; pick one per 30-day window. |

Several other top entries (HTML "design skill" repos, prompt-collection repos, `kyegomez/OpenMythos`) handle no untrusted input and would have been skipped on the "Handles untrusted input" criterion.

**No repo was forked.** No entry was written to `memory/vuln-scanned.json` (file was created empty so future runs have the structure in place).

## What needs to land before the next Saturday run

1. Add `scripts/prefetch-vuln-scanner.sh` modeled on `prefetch-xai.sh`. It must run before Claude starts (workflow step with full network access) and install `semgrep` (`pip install --user`), and pull `trufflehog` + `osv-scanner` release binaries into a stable on-repo path (e.g. `.vuln-scan/bin/`).
2. Update `skills/vuln-scanner/SKILL.md` step 3 to invoke binaries by their cached path (`.vuln-scan/bin/<tool>`) rather than `pip install` / `curl | sh` at runtime.
3. Add a `.vuln-scan/.gitkeep` so Claude can write inside that directory without hitting the sandbox's create-directory deny rule.

Once those land, `ISS-001` resolves and the next Saturday run produces real output.

## Sources status

- semgrep = fail
- trufflehog = fail
- osv-scanner = fail
- slither = fail (not attempted; no Solidity in any candidate)
- gh trending API = ok (used to enumerate candidates)
