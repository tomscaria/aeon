---
name: project-rolr-competition
description: ROLR freeplay paper competition — public tRPC API mirrors Polymarket; $0 scanner cron live; phase-2 execution via positions.buy + session cookie pending
metadata: 
  node_type: memory
  type: project
  originSessionId: 5fc03df3-4b62-47f2-b75a-b1298de97449
---

ROLR paper-trading competition (freeplay.rolr.com). Discovered 2026-06-10: **no scraping needed** — public unauthenticated tRPC `GET /api/trpc/markets.list` (limit≤50, cursor pagination, volume-sorted, ~2,000 markets) mirrors Polymarket: outcome `externalId` = gamma market id (binary markets use `<id>-yes`/`<id>-no` stems), full shape also carries CLOB `tokenId` (lite shape omits it — alternates between responses). `staleness`/`fetchedAt` fields expose ROLR's own cache lag = the edge.

Shipped commit `910c654a`: `python/scanner/rolr_client.py` + `scripts/rolr_arb_scan.py` + LaunchAgent `ai.rswarm.rolr-arb` (300s, LIVE since 2026-06-10). Gamma batch quotes primary, CLOB `/prices` fallback (CLOB WAF 403s without browser User-Agent — not a geoblock). History in `data/rolr_scan.jsonl` (run_id per line, for edge-decay/cadence tuning), report `outputs/YYYY-MM-DD_rolr_arb.md`, Telegram ≥4% edge with 30-min slug dedupe. First live scan: 9 executable edges, max 41.5%.

**Official Rules read 2026-06-11 ($25M Challenge, Jun 2–Aug 12 2026):** real cash prizes ($1k/wk, $5k/mo, $25M grand via 7-question final, $75k season champion fallback). **Eligibility is residency-based, not IP** — SF/CA eligible (excluded: AZ IL MD MA MI NV NJ OH WA); NYC IP irrelevant; KYC required for Finalists by Jul 31. **Hard cap: 10 predictions/day** (resets 12:00 AM EST); leaderboard = weekly Delta Points (mark-to-market, updated ~24h); weekly cutoff Sun 12:00 PM EST; weekly top-3 become Finalists. Site ToS bans "automated means, including bots or scrapers"; Official Rules §20 allows DQ for "tampering" at sole discretion. **Phase 2 auto-execution via positions.buy KILLED** — DQ risk with real prizes + 10/day cap makes manual execution from Telegram alerts strictly better. Scanner = decision support; founder places trades by hand.

**Decisions 2026-06-11:** (1) Founder WILL DISCLOSE to ROLR that he scans their app — he's interviewing to lead their prediction-markets business, so disclosure converts "weird if I won" into the interview demo. (2) No auto-execution, confirmed. (3) Cadence to drop from 5min → goldilocks (~15min, staleness-gated): the alpha is ROLR's price *lag* vs Polymarket, not scan speed — ROLR tRPC response carries `staleness`/`fetchedAt`, so polling faster than ROLR refreshes is wasted calls AND the cover story ("I poll at ROLR's own refresh rate"). Surface `fetchedAt` age in alerts as both quality filter and narrative. Related: [[polymarket-datacenter-ban]].

**SHIPPED 2026-06-11 (cadence + sizing, uncommitted):** `scripts/rolr_arb_scan.py` — (1) plist 300→900s (15min), copied to `~/Library/LaunchAgents/` + reloaded (PID 22205, StartInterval=900 verified); (2) active-hours guard `within_active_hours()` window 09:00–01:00 ET, gated BEFORE ROLR fetch (`--active-start/--active-end/--ignore-hours`); (3) staleness gate `rolr_fingerprint()` — sha256 of sorted (slug,outcome,price@4dp); skips PM sweep when ROLR prices byte-identical to last run (state in `data/.rolr_arb_fingerprint.json`, `--force` bypasses). **Gate fired live on first reload** ("ROLR cache unchanged — skipping PM sweep") confirming ROLR refreshes slower than old 5min cadence; (4) edge-weighted concentrated sizing `suggest_stake(edge,bankroll)` = `max_frac·(edge/target_edge)` clamped [0.03,0.20], target_edge=0.25, default bankroll=250cr (`--bankroll`); (5) enriched alert `_format_alert_row()`: "BUY/SELL ROLR … → target {pm_mid} … edge X% · stake Ncr · {staleness}". 16 new tests `tests/test_rolr_arb_scan.py` all green. **Feature 3 SHIPPED 2026-06-11 (commit `c21ceb50` on main, +ADR-139, NOT pushed):** new `python/scanner/rolr_positions.py` — `RolrPosition` store at `data/rolr_positions.json`, `open_position/close_position/get_open`, `realized_delta()` (binary-side P&L), pure `evaluate_position()` → (holding|converged|stale, progress) with CONVERGE_TOL=0.02 (TP=gap closed, SL=PM moved through entry; NOT a %-drawdown stop). `market_url(slug)` = `https://freeplay.rolr.com/markets/<slug>` (verified 200). Scanner: `monitor_positions(markets,prices)` folded into scan (matches open positions by slug+outcome-substr, pings once per converged/stale transition, persists `monitor_state`); alert rows now carry tappable `<a>open market</a>` link + pre-typed `/rolr_buy|sell …` log command. Telegram: 4 new handlers `/rolr_buy /rolr_sell /rolr_close /rolr_positions` (`python/alerting/telegram.py`, registered + in /help). 19 new tests (`test_rolr_positions.py` 16 + monitor-integration 3), 35 total green. **Preload-trade-in-quote-entry = IMPOSSIBLE** (verified by reading ROLR's JS bundle: trade form's `presetAmountCents` is in-app click state, not a URL param; only query params the app reads are `bucket`+auth/tracking). Deepest reliable link = market page.

**Onboarding UX SHIPPED 2026-06-11 (commit `84157231` on origin/main):** bot made usable cold — `/start` real welcome (was alias to 37-cmd /help), `/rolr` plain-language Challenge playbook (numbered 1-5 + open positions inline), curated `/`-menu via `setMyCommands` (ROLR-first ~12 entries, the typeahead popup = top discovery surface), friendlier log confirmation ("I'll ping you when to close") + example-first usage errors ("copy the command from the alert"). All in `python/alerting/telegram.py`, activates on next loop restart. **ADR-139 NUMBER COLLISION on origin:** background agent `c3738298` reused ADR-139 (Execution Station, DECISIONS.md L2992) clashing with ROLR ADR-139 (L2937, `c21ceb50`); background family also took ADR-140 (Dual-mode L1). Recommended fix = renumber Execution Station → ADR-141 (140 taken). UNRESOLVED — founder to decide.

**ACTIVATION STATE:** Outbound (enriched alerts + monitor pings) = LIVE on next 15-min scan (launchd runs script fresh, pings via one-shot send_message_sync). Inbound `/rolr_*` commands = NOT live until trading-loop restart — the telegram bot is a `subprocess.Popen` CHILD of `python/main.py` (loop PID 70803→bot PID 70861), spawned once at boot with NO respawn watcher. Killing the bot just loses it. Activating inbound needs an `ai.rswarm.trading-loop` bounce (safe per completed runner-canary remediation `598ae1c5`, default canary OFF) — deferred to founder.

**Repo facts (Explore 2026-06-11):** Scanner = `scripts/rolr_arb_scan.py` (alerts only, no sizing, top-5, 30min dedupe). Schedule = `infra/launchd/ai.rswarm.rolr-arb.plist` StartInterval=300 (5min). ROLR client = `python/scanner/rolr_client.py` (tRPC `markets.list`, returns `staleness`/`fetchedAt`). TP/SL: only `calibration_gap.py:354 check_stop_losses()` (PM/HL live, 5% CG_STOP_LOSS_PCT, NOT wired to ROLR); NO take-profit engine anywhere. Trade journaling: `TradeRecord` in `python/agents/base.py:37` auto-records agent trades (`exit_reason` ∈ resolved/stop_loss/manual) — but NO manual-trade logging command exists. Telegram handlers in `python/alerting/telegram.py:593+` (37 cmds, pattern = `CommandHandler("name", fn)` at L1654+); no `/rolr_log`.
